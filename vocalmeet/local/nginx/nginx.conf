events {
    worker_connections 1024;
}

http {
    # Upstream WordPress
    upstream wordpress {
        server wordpress:80;
    }

    # Redirect HTTP to HTTPS
    server {
        listen 80;
        server_name vocalmeet.local;
        return 301 https://$server_name$request_uri;
    }

    # HTTPS Server
    server {
        listen 443 ssl;
        server_name vocalmeet.local;

        # SSL Configuration
        ssl_certificate /etc/nginx/ssl/vocalmeet.local.crt;
        ssl_certificate_key /etc/nginx/ssl/vocalmeet.local.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # Proxy settings
        location / {
            proxy_pass http://wordpress;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Port $server_port;
            
            # WebSocket support (for Elementor live preview)
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            
            # Timeouts
            proxy_connect_timeout 60s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
            
            # Buffer settings
            proxy_buffer_size 128k;
            proxy_buffers 4 256k;
            proxy_busy_buffers_size 256k;
        }

        # Increase max upload size for WordPress
        client_max_body_size 100M;
    }
}
